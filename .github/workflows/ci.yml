name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build_and_test:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
          cache-dependency-path: |
            library-app/package-lock.json
            backend/package-lock.json

      - name: FE Install Dependencies # Строка 38 (проверь отступ - ровно 2 пробела перед дефисом)
        run: npm ci
        working-directory: ./library-app

      - name: FE Build
        run: npm run build --if-present
        working-directory: ./library-app

      - name: FE Test
        run: npm test
        working-directory: ./library-app
        env:
          CI: true

      - name: BE Install Dependencies
        run: npm ci
        working-directory: ./backend

      # Закомментированный шаг для тестов бэкенда (требует доп. настройки)
      # - name: BE Test
      #   run: npm test
      #   working-directory: ./backend
      #   env:
      #     CI: true
